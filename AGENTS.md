This is a static website built with Jekyll. It uses the Minima 3.0
theme via a remote GitHub fetch, and uses Bundler for dependency
management. It is deployed via GitHub Actions to GitHub Pages. All
styling should be done using standard Jekyll SCSS overrides in the
assets folder. Do not use Node.js, npm, or React.
